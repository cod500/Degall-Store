<nav class="navbar navbar-expand-lg navbar-light bg-secondary">
  <a class="navbar-brand" href="/">Shop Degallerie</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
    aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link" href="/">Home</a>
      {{#each pages}}
      <a class="nav-item nav-link" href="{{slug}}">{{title}}</a>
      {{/each}}
    </div>
  </div>
  {{#if user}}
    <ul class="nav-item nav-link ml-auto">
    <a class="" href="/cart/checkout">
      Cart( {{#if cart}}{{cart.length}}{{else}}0{{/if}} )</a>
  </ul>
   <ul class="nav-item nav-link ml-auto">
    <a class="nav-item nav-link" href="/users/logout">Sign out</a>
  </ul>
  {{else}}
   <ul class="nav-item nav-link ml-auto">
    <a class="nav-item nav-link" href="/users/register">Register</a>
  </ul>
  <ul class="nav-item nav-link ml-auto">
    <a class="nav-item nav-link" href="/users/login">Sign in</a>
  </ul>
  {{/if}}
</nav>